services:
  - type: web
    name: college-management-system
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: CMS_DB_URL
        sync: false
      - key: CMS_DB_USER
        sync: false
      - key: CMS_DB_PASSWORD
        sync: false
